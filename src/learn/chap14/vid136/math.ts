

export const sum = (a: number, b: number) => {
    return a + b;
}

//subtract : phép trừ
export const subtract = (a: number, b: number) => {
    return a - b;
}

// 2 cách export: export cuối file hoặc export trực tiếp chính hàm/biến đó
// export { sum, subtract };